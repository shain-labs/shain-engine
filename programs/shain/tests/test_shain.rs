use anchor_lang::prelude::*;

#[test]
fn initialize_sets_config() {
    // config fields + authority asserted in later revision
}

#[test]
fn start_session_happy_path() {
    // holder + ata + start_session asserted in later revision
}
